apiVersion: v1
kind: Service
metadata:
  name: {{ include "xuangong-app.fullname" . }}
  labels:
    {{- include "xuangong-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xuangong-app.selectorLabels" . | nindent 4 }}
